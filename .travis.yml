language: java

services:
  - docker

jdk:
  - oraclejdk8

before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -y python-pip

install:
  - mvn -DskipTests=true install

script: 
  - mvn test
  - mvn package

spring:
  jpa.hibernate:
    ddl-auto: update
  datasource:
    url: jdbc:mysql://localhost/jandy
    username: root
    password:
    driver-class-name: com.mysql.jdbc.Driver
    continue-on-error: true